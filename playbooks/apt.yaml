- hosts: "pihole"
  become: yes
  tasks:
    - name: Add an Apt signing key, uses whichever key is at the URL
      ansible.builtin.apt_key:
        url: https://packages.cloud.google.com/apt/doc/apt-key.gpg
        state: present  
    - name: apt
      apt:
#        update_cache: yes       failing with error
        upgrade: 'yes'  
  